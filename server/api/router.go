package api

import (
	"github.com/gofiber/fiber/v2"
	//"github.com/nynrathod/email-microservice/internal/email"
	//"github.com/nynrathod/email-microservice/config"
)

func SetupRoutes(app *fiber.App) {
	//emailGroup := app.Group("/email")

	// Initialize dependencies inside router
	//db := config.GetDB()
	//emailRepo := email.NewRepository(db)
	//emailService := email.NewService(emailRepo)

	//email.SetupEmailRoutes(emailGroup, emailService)
}
